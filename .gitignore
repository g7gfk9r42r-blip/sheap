# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Gradle caches (never commit)
**/.gradle/

# Node.js related
node_modules/
server/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Server generated/persistent data (DO NOT COMMIT)
server/media/
server/data/
server/dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase config (local only) - prefer NOT committing these for public repos
firebase/Apple/GoogleService-Info.plist
firebase/Google/google-services.json
apple/GoogleService_Info.plist
google/google_services.json
apple/GoogleService-Info.plist
google/google-services.json

# Common Flutter Firebase locations (local only)
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
macos/Runner/GoogleService-Info.plist

# Android signing (NEVER commit)
android/key.properties
android/app/key.properties
**/*.jks
**/*.keystore
**/*.p12
**/*.mobileprovision

# CocoaPods (never commit)
ios/Pods/
macos/Pods/

# IDE and editor files
.vscode/settings.json
.vscode/launch.json
.vscode/extensions.json

# Generated data files and logs
/data/
/logs/
# Prospect PDFs are generated/downloaded artifacts (do not commit)
assets/prospekte/**/*.pdf
# NOTE: Do NOT ignore all *.json globally; recipe/prospekte assets + server media are JSON.
# Keep JSON tracked; ignore only known generated/secret JSON via explicit patterns above.

.DS_Store
**/.DS_Store

# Android release signing
android/key.properties
android/keystore/

# CocoaPods (never commit)
ios/Pods/
macos/Pods/

