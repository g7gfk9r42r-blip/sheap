# Grocify 2.0 – Environment Configuration Template
# Copy this file to `.env` and fill in with your actual values.
# NEVER commit `.env` to version control!

# === Firebase Configuration ===
# If you use Firebase locally, provide these settings.
# Leave empty or comment out to disable Firebase (app runs in offline mode).
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_MEASUREMENT_ID=your_measurement_id

# === Backend / Media Server ===
# Set this to the URL of your backend server (e.g., http://localhost:3000 or https://api.example.com)
# Used by the app to fetch recipes and media (recipe images, weekly offers).
# If empty, the app falls back to bundled assets (useful for demos without a live server).
API_BASE_URL=http://localhost:3000

# === Admin Secret ===
# Required to call protected endpoints on the backend:
#   - POST /admin/refresh-offers
#   - POST /admin/refresh-recipes
#   - POST /admin/upload-media-tar
# Only needed if you're running the backend locally for development.
ADMIN_SECRET=your_admin_secret_here

# === OpenAI API Key ===
# Used by `tools/weekly_pro.py` to generate recipe descriptions and by
# `assets/prospekte/lidl/extract_lidl_offers_vision.py` for OCR/vision extraction.
# Get your key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-proj-your_openai_key_here

# === Optional: Replicate API Token ===
# Alternative image generation backend (optional).
# Used by some recipe generation scripts.
REPLICATE_API_TOKEN=your_replicate_token_here

# === Server Mode (for backend) ===
# Set to "production" for live deployments; "development" for local work.
NODE_ENV=development

# === Database (Server) ===
# SQLite database file path for the backend (created automatically if missing).
DATABASE_URL=./data/grocify.db

# === Logging ===
# Set to 'debug' or 'info' for verbose logging in server.
LOG_LEVEL=debug
